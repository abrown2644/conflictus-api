@page
@model Conflictus.Pages.Wars.IndexModel

@{
    ViewData["Title"] = "Index";
}
<br />
<div class="container row p-0 m-0">
    <div class="col-md-10 col-8">
        <h2 class="text-dark">Wars</h2>
    </div>
    <div class="col-md-2 col-4">
        <a asp-page="Create" class="btn btn-primary form-control text-white">New War</a>
    </div>
    @*<div class="wars row">
            @foreach (var war in Model.War)
            {
                <div class="col-md-4 col-12">
                    <a class="war-link" asp-page="./Details" asp-route-id="@war.Id">
                        <div class="war-card" style="background:url('@war.ImageUrl') 0 no-repeat; background-size:cover;">
                            <h2 class="text-center text-white font-weight-bold">@war.Name</h2>
                            <div class="options-wrapper">
                                @*<div class="war-options">
                                        <a title="View" asp-page="./Details" asp-route-id="@war.Id">👁️‍🗨️</a>
                                        <a title="Edit" asp-page="./Edit" asp-route-id="@war.Id">🛠️</a>
                                        <a title="Delete" asp-page="./Delete" asp-route-id="@war.Id">⛔</a>
                                    </div>
                            </div>
                        </div>
                    </a>
                </div>
        @foreach (var battle in item.Battles)
        {
            <p>@battle.Title</p>
            foreach (var partA in battle.SideA.Participants)
            {
                <img src="@partA.FlagUrl" />
                <p>@partA.Name</p>
            }
        }

            }
        </div>*@
    <div class="col-12 p-3 mt-3">
        <table id="WDT_load" class="table table-striped table-bordered display compact" style="width:100%">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Name</th>
                    <th>Options</th>
                </tr>
            </thead>
        </table>
    </div>
</div>
@*<a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>*@

@section Scripts{
    <script src="~/js/warList.js"></script>
}
